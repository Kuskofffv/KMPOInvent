name: brigantina_invent_workspace
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  yaml: any
  yaml_edit: ^2.1.0
  crypto: ^3.0.4

dependency_overrides:
  #xml: ^5.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0
  melos: ^6.1.0